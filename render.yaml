services:
  - type: web
    name: litepick-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python litebot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
